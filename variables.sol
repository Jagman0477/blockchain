pragma solidity > 0.7.0 < 0.9.0;

contract vraibles{
    
    // uint choclateBars = 10;
    // uint storeOwner = 300;
    // bool lieDetector = true;
    // string errorMessage = "There is an error";
    uint wallet = 500;
    bool spend = false;
    string notifySpend = "You have spent money";

}